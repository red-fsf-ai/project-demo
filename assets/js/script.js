$(document).ready(function(){
    var noteCount = 0;
    var activeNote = null;
    // colour
    // save
        // on click
        // capture the value of the title
        // capture the value of the note body
        // create a new date object
        // create a new element that contains the title, note, and date of the note and append it to a note list


        // validate that the note isn't empty before saving
        // keep track of what note you're working with
    // delete
    // display
});